import * as React from "react";
import { UploadProps } from "./types";
export default function Upload({ disabled, multiple, hideInput, previewFileAsFiles, error, helperText, file, onDelete, files, thumbnail, onUpload, onRemove, onRemoveAll, sx, ...other }: UploadProps): React.JSX.Element;
