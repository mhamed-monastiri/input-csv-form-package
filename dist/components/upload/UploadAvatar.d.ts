import * as React from "react";
import { UploadProps } from "./types";
export default function UploadAvatar({ ModelImage, error, file, disabled, helperText, sx, ...other }: UploadProps): React.JSX.Element;
