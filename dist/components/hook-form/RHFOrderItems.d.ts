import * as React from "react";
type Props = {
    name: string;
    fields: any[];
    label?: string;
    helperText?: React.ReactNode;
};
export default function RHFOrderItems({ fields, name, label, helperText, }: Props): React.JSX.Element;
export {};
